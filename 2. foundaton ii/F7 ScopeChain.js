var name = "kiran";

function printName() {
  console.log(name); // will print kiran
  // var name = "kiran"; // if we add this line above console.log will print undefind beacuse it will host name variable undefind in this scope
}

printName();
